# tools
Todo for U05:
 + fix window resize, right now resize doesn't do anything
 + Render to texture for docking
   - flytta till U06
 - file-system-watcher implementation so we can hot-swap/hot-load
 - file-system parsing
 - http request for assets?

Från U04 behöver vi fixa:
 x Multi-selection
 - Transform för selection
 	x position
	- rotation
	- scale
 - Hierarki
 	- Kunna ha parent/child förhållande mellan ModelInstances
	- Transforms på barn om föräldern transformar

Flyttas till senare:
 - fix window resize, right now resize doesn't do anything
 + Render to texture for docking
